# vklib

Vulkan abstraction layer library written in C.
Depends only on volk, rest of the libraries are for the sample program.

## Usage
I'm lazy to write docs, read the sample program its simple enough xd.
